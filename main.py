#import numpy
import data_ops
#import model_ops


# (x_train, y_train), (x_test, y_test) = data_ops.load_minst_data()

my_data = data_ops.load_my_data()
#model = model_ops.load()
#results = model.predict(my_data)

#for result in results:
    #print(numpy.argmax(result))
